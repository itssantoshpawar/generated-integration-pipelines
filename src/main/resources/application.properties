spring.application.name=osb-pipeline-orchestration
server.port=8080

# Logging configuration
logging.level.com.osb.integration=DEBUG
logging.level.org.springframework=INFO

# Flow configuration
spring.config.import=optional:classpath:flow-config.yml
